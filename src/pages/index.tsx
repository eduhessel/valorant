import styles from 'styles/home.module.scss'

const HomePage = () => (
  <div className={styles.container}>
    <header>
      <div>header</div>
    </header>
    <main>main</main>
  </div>
)

export default HomePage
